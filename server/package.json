{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "rimraf": "^2.6.1",
    "typescript": "^2.4.2"
  },
  "scripts": {
    "go": "rimraf dist && tsc --traceResolution && node ./dist/server.js"
  }
}
